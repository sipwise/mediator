#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@

# We keep it to cleanup the old init script setup.
override_dh_installinit:
	dh_installinit --name=mediator

.PHONY: override_dh_installinit
